<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Foreground rounded “pill” (the visible bar) -->
    <item
        android:left="12dp"
        android:right="12dp"
        android:bottom="8dp">
        <shape android:shape="rectangle">
            <corners android:radius="28dp" />
            <solid android:color="@color/nav_background_color" />
        </shape>
    </item>

</layer-list>
